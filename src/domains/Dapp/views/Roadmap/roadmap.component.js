import { Fragment } from 'react';

import { Timeline } from '@core/components';
import { Section } from '@dapp/components';
import { StyledRoadmap } from './roadmap.styles';

const Roadmap = () => {

    return (
        <Fragment>
            <Section label="Q1 2021">
                
			</Section>
        </Fragment>
    );
};

export default Roadmap;