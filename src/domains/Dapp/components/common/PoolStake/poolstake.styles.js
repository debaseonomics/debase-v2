import styled from 'styled-components';

export const StyledPoolStake = styled.div`
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 20px;
`;

export const StyledCardInner = styled.div`
    gap: 0;
`;

export const StyledAprText = styled.div`
    gap: 0px;
`;