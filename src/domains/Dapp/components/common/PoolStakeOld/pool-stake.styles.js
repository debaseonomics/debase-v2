import styled from 'styled-components';

export const StyledPoolStake = styled.div`
    position: relative;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    gap: 30px;
`;

export const StyledCardInner = styled.div`
    gap: 0;
    width: 100%;
`;

export const StyledInfo = styled.div`
    position: relative;
`;

export const StyledStakeForms = styled.div`
    position: relative;
`;

export const StyledClaimActions = styled.div`
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin-top: auto;
`;
