import { StyledTimeline } from './timeline.styles';

const Timeline = ({ data }) => {
    return (
        <StyledTimeline>
            timeline
        </StyledTimeline>
    );
};

export default Timeline;